server:
  port:8888

spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.home}\\D:\\Study\\native-file-repo
        git:
          uri: https://github.com/Sanizzang/spring-cloud-config
